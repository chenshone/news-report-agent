# Core LLM keys
# Option 1: OpenAI (推荐用于开发和测试)
OPENAI_API_KEY=YOUR_OPENAI_KEY

# Option 2: Azure OpenAI (企业部署推荐)
AZURE_OPENAI_API_KEY=YOUR_AZURE_KEY
AZURE_OPENAI_ENDPOINT=YOUR_AZURE_ENDPOINT
AZURE_OPENAI_DEPLOYMENT_NAME=YOUR_AZURE_DEPLOYMENT

# NOTE: 
# - Azure 通过 DEPLOYMENT_NAME 来确定使用的模型，不需要额外的版本配置
# - 如果同时配置了 OPENAI_API_KEY 和 Azure 配置，系统会优先使用 Azure
# - 如果只配置 OPENAI_API_KEY，则使用 OpenAI
# - 对于开发测试，推荐使用 OpenAI（配置简单）
# - 对于生产环境，推荐使用 Azure（更稳定、更安全）

# Search and crawl
TAVILY_API_KEY=YOUR_TAVILY_KEY
BRAVE_API_KEY=YOUR_BRAVE_KEY
FIRECRAWL_API_KEY=YOUR_FIRECRAWL_KEY

# Optional: override agent filesystem base directory
NEWS_AGENT_FS_BASE=./data

# ==============================================================================
# Google Gemini (用于专家 Agent)
# ==============================================================================
# 如果配置了 GEMINI_KEY 和 MODEL_GEMINI_3_FLASH，专家 Agent（summarizer, 
# fact_checker, researcher, impact_assessor）会使用 Gemini 模型，
# 而 Master Agent 仍使用 OpenAI/Azure 模型。
#
# 获取 API Key: https://aistudio.google.com/app/apikey
# 
# 支持的模型:
#   - gemini-2.5-flash (最新，推荐)
#   - gemini-2.5-flash-preview-05-20 (预览版)
#   - gemini-2.0-flash
#   - gemini-1.5-flash
#   - gemini-1.5-pro
# ==============================================================================
GEMINI_KEY=YOUR_GEMINI_KEY
MODEL_GEMINI_3_FLASH=gemini-2.5-flash