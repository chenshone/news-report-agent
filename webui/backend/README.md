# Web UI Backend

FastAPI backend for the news report agent web interface.

## Setup

```bash
cd webui/backend
uv pip install -r requirements.txt
```

## Run

```bash
uv run uvicorn main:app --reload --port 8000
```
